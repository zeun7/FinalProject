<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="multi.mall.com.member">

	<select id="SELECT_ALL" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member order by num desc
	</select>
	
	<select id="SELECT_ONE" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where num = #{num}
	</select>
	
	<select id="SELECT_ONE_ID" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where id = #{id}
	</select>
	
	<select id="ID_CHECK" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where id = #{id}
	</select>
	
	<select id="LOGIN" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where id = #{id} and pw = #{pw}
	</select>
	
	<select id="SEARCH_LIST_ID" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where id like #{searchWord}
	</select>
	
	<select id="SEARCH_LIST_NAME" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where name like #{searchWord}
	</select>
	
	<select id="SEARCH_LIST_ADDR" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where addr like #{searchWord}
	</select>
	
	<select id="SEARCH_LIST_TEL" resultType="multi.mall.com.member.model.MemberVO">
		select * from mm_member where tel like #{searchWord}
	</select>
	
	<insert id="INSERT">
		insert into mm_member(num, id, pw, name, addr, tel) 
		values(seq_mm_member.nextval, #{id}, #{pw}, #{name}, #{addr}, #{tel})
	</insert>
	
	<update id="UPDATE">
		update mm_member set pw = #{pw}, name = #{name}, addr = #{addr}, tel = #{tel} 
		where num = #{num}
	</update>
	
	<delete id="DELETE">
		delete from mm_member where num = #{num}
	</delete>
	
	<update id="M_BUY">
		update mm_member set point = point + #{point} where id = #{id}
	</update>
</mapper>